general:
  branches:
    only:
      - master

dependencies:
  pre:
    - git config --global user.name "CircleCI"
    - git config --global user.email "circleci@example.com"
  override:
    - sudo pip install sphinx
    - sudo pip install yogosyu

test:
  override:
    - exit

deployment:
  master:
    branch: master
    commands:
      - sh make.sh circle
